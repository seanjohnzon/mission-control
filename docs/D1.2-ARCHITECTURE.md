# D1.2 — ARCHITECTURE
**Autonomous AI Organization — Canonical System Map**
_Last updated: 2026-03-13 | Status: Active | Parent: D1.1-ROADMAP.md_

---

## 1. Purpose

This document is the canonical structural map of the system. It describes what machines exist, what runs on them, how they connect, and how the layers of the organization fit together.

**What this document anchors:**
- Which machines hold which runtime agents
- What the network looks like now vs. what it must become
- Where state, memory, and config live by machine
- How Mission Control, Telegram (primary channel), local inference, and tooling relate to each other
- What is decided vs. what is still open

**What this document is not:**
- It is not a requirements spec (see D1.3)
- It is not a policy document (see D1.5–D1.15)
- It is not a runbook (see D2.x)
- It does not define org roles or decision rights (see D1.4)

When any structural decision changes — a machine is added, a network path changes, an agent moves — this document is updated first. Downstream documents that reference architecture must be flagged for review when this file changes.

---

## 2. System Mission Context

The mission is to build and operate an autonomous organization of AI agents that produces real value 24 hours a day without requiring constant human attention. The human role is architect, director, and judge — not operator.

That mission requires a physical substrate. Agents don't run in the abstract — they run on machines, consume memory, use network paths, and call tools. This document describes that substrate: the actual hardware, the actual network, and the actual software topology the organization runs on.

The architecture is built to be durable before it is clever. Machines must stay reachable. State must not be lost. Local inference must work even when cloud APIs are unavailable. The communication layer must be async-first. Mission Control must remain the source of truth regardless of what else changes.

---

## 3. Machine Inventory

### MacBook Air — Control Hub / HQ

| Field | Value |
|---|---|
| Role | Control Hub — human interface, directive entry, document authoring |
| Current status | Active. Primary working machine. Remote access to Mac Mini confirmed. |
| Intended status | Portable HQ. Not a worker node. Not always online. |
| OS | macOS (version TBD) |
| Location | Mobile — follows the human |
| Network | Home Wi-Fi + wherever human travels |
| Key constraints | Not always on. Not a server. Must not be a dependency for autonomous operation. The org continues when the MacBook Air is closed. |
| OpenClaw | Client only — used to issue directives and monitor status, not to run gateway or agents |
| Cursor | Primary authoring and code environment |
| Mission Control access | Read/write access to the workspace hosted on Mac Mini. The human authors, reviews, and edits from here — but the authoritative copy lives on Mac Mini, not on this machine. |

---

### Mac Mini — Bridge Node

| Field | Value |
|---|---|
| Role | Bridge — routing, scheduling, comms brokering, gateway management, first always-on node |
| Current status | Active. OpenClaw 2026.3.7 installed. Gateway running under launchd. Remote access working via Wi-Fi. |
| Intended status | Primary 24/7 anchor node. Ethernet-connected to basement cluster. Gateway and bridge agent running permanently. |
| OS | macOS 26.2 (Apple Silicon — arm64) |
| Chip | Apple Silicon (ARM64) |
| Location | Basement (fixed) |
| Network — current | Wi-Fi only: `10.0.0.167` (en1). Ethernet (en0) physically connected to switch but no DHCP lease — isolated switch segment. |
| Network — target | Ethernet primary with static DHCP reservation. Wi-Fi as backup. |
| Key constraints | Must never sleep. Must restart after power failure. Ethernet must get a proper LAN IP before this is considered complete. |
| OpenClaw | Gateway host. bridge agent runtime. State: `~/.openclaw/`. Gateway port: `18789`. LaunchAgent: `~/Library/LaunchAgents/ai.openclaw.gateway.plist` |
| Node runtime | Node 22.14.0 at `~/.local/node22/bin/` |
| Cursor | Secondary — may be used for remote editing sessions |
| Local LLM | Required. Model TBD. Must run locally for bridge-level tasks. |
| Mission Control workspace | Hosted here initially. Path: `~/Cursor Projects/mission-control/` |

---

### Desktop — Brain Node

| Field | Value |
|---|---|
| Role | Brain — memory, planning, commerce logic, knowledge synthesis |
| Current status | Not yet integrated. Physical machine exists in basement. Legacy n8n workflows and prior automation assets present. |
| Intended status | Always-on worker node. brain agent runtime. Canonical memory host. Commerce logic engine. |
| OS | TBD (likely Windows or Linux — to be confirmed on integration) |
| Location | Basement (fixed) |
| Network — current | Connected to basement switch. No LAN IP until switch is uplinked. |
| Network — target | Ethernet with static DHCP reservation on home LAN. |
| Key constraints | Hosts the canonical memory store — this machine's data integrity is critical. Must have sufficient RAM for memory indexing and local LLM inference. |
| OpenClaw | brain agent runtime. Paired to bridge gateway on Mac Mini. |
| Local LLM | Required. Model TBD — must support memory embedding and planning tasks. |
| Legacy assets | n8n workflows, commerce automation, prior bot config, dashboard concepts, prompt packs. Must be inventoried before integration begins. See D5.3. All discovered legacy assets remain inactive by default until classified as keep, adapt, or retire through the migration process. Nothing runs just because it exists. |
| Mission Control | Read/write access as brain agent. Not the host of Mission Control itself. |

---

### GPU3060 Laptop — Forge Node

| Field | Value |
|---|---|
| Role | Forge — build, QA, research, local inference workloads |
| Current status | Not yet integrated. Physical machine exists in basement. |
| Intended status | Always-on worker node. forge agent runtime. GPU-accelerated local inference host. |
| OS | TBD (likely Windows or Linux — to be confirmed on integration) |
| GPU | NVIDIA RTX 3060 |
| Location | Basement (fixed) |
| Network — current | Connected to basement switch. No LAN IP until switch is uplinked. |
| Network — target | Ethernet with static DHCP reservation on home LAN. |
| Key constraints | GPU is the primary asset — must be available for local inference, not consumed by background processes. Heat and power management matter for always-on operation. |
| OpenClaw | forge agent runtime. Paired to bridge gateway on Mac Mini. |
| Local LLM | Required. GPU-accelerated. Model TBD — must support code generation, QA, and research summarization. Primary inference node for high-load tasks. |
| Mission Control | Read/write access as forge agent. |

---

### Pixel — Edge Execution Surface

| Field | Value |
|---|---|
| Role | Edge execution — mobile task execution, notifications, monitoring |
| Current status | Not integrated. Form factor and exact capabilities not yet pinned. |
| Intended status | Edge node. May receive delegated tasks from bridge. Not a primary runtime agent host in Phase 1–2. |
| Key constraints | Mobile device — not always on, not always reachable. Must not be a dependency for core org operation. |
| Agent form | TBD — may pair as a device to bridge rather than running its own OpenClaw agent |

---

### Seeker — Edge Execution Surface

| Field | Value |
|---|---|
| Role | Edge execution — TBD |
| Current status | Not integrated. Role not yet fully defined. |
| Intended status | Edge surface. Likely similar pattern to Pixel — paired to bridge, not an independent agent runtime. |
| Key constraints | TBD |
| Agent form | TBD |

---

## 4. Runtime Architecture

### The Three Real Agents

At launch, the organization runs exactly three OpenClaw runtime agents:

```
bridge   →  Mac Mini       Routing, scheduling, comms brokering, gateway management
brain    →  Desktop        Memory, planning, commerce, knowledge synthesis
forge    →  GPU3060        Build, QA, research, local inference workloads
```

Each agent runs on one machine. Each agent has one OpenClaw workspace. Each agent has one model configuration. Each agent has bounded, documented responsibilities.

### Logical Roles Live Inside Agents — They Are Not Separate Agents

Named roles like Conductor, Broker, Archivist, Planner, Merchant, Builder, Critic, and Researcher are **logical labels** — they describe what a given agent is doing in a given prompt context. They are not separate OpenClaw agents, not separate processes, and not separate instances.

| Role | Lives In | What it does |
|---|---|---|
| Conductor | bridge | Coordinates task flow across agents |
| Broker | bridge | Routes messages, manages channel I/O |
| Archivist | brain | Maintains memory, indexes knowledge |
| Planner | brain | Produces plans, tracks goal state |
| Merchant | brain | Commerce logic, value tracking |
| Builder | forge | Code generation and execution |
| Critic | forge | QA, testing, evaluation |
| Researcher | forge | Reads sources, extracts signal |

### Why Agent Count Is Low at First

Fewer agents means fewer failure modes. Each agent is a runtime process on a real machine with real memory, real costs, and real failure modes. Adding a new agent before the existing agents have a proven track record adds complexity that compounds — coordination overhead, more things to monitor, more things to recover when they fail.

The logical role model gives us the flexibility of many specialized behaviors while keeping the operational footprint small. When a new capability is genuinely needed and cannot be absorbed into an existing agent's role, adding a fourth agent becomes a considered architectural decision — not an impulse.

---

## 5. Current Network Topology

_This is the actual current state as of 2026-03-11. It is not the target. It is documented honestly._

```
Internet
    │
  Home Router (10.0.0.1)
    │
  Home LAN: 10.0.0.0/24
    │
    ├── Mac Mini (Wi-Fi, en1): 10.0.0.167   ← active connection, remote access working
    └── [other home devices on 10.0.0.x]

  Basement Switch (ISOLATED — no router uplink):
    ├── Mac Mini (Ethernet, en0): 169.254.91.102   ← link active, cable connected, no DHCP
    ├── Desktop:   APIPA / unreachable              ← not yet powered on / integrated
    └── GPU3060:   APIPA / unreachable              ← not yet powered on / integrated
```

**Key facts about current state:**
- The Mac Mini is reachable only via Wi-Fi (`10.0.0.167`). Ethernet is physically wired but not routed.
- The basement switch has no uplink cable to the router. It is an isolated Layer 2 segment.
- The Desktop and GPU3060 are physically on the switch but have produced no network traffic — they are either off or not yet configured.
- IP forwarding is disabled on the Mac Mini. Internet Sharing is not configured. The Mac Mini is not acting as a gateway.
- The MacBook Air reaches the Mac Mini via home Wi-Fi at `10.0.0.167`.
- SSH is listening on port 22 (socket-activated by launchd). Screen Sharing daemon is running.
- The OpenClaw gateway listens on `ws://127.0.0.1:18789` — loopback only. Only local clients on the Mac Mini can currently connect.

**What is not finalized:**
- The switch has no uplink. One cable fixes this but it has not been run yet.
- The Mac Mini has no stable Ethernet IP. DHCP reservation has not been set.
- Desktop and GPU3060 have no LAN presence.

---

## 6. Target Network Topology

_This is the intended architecture once the basement cluster is properly connected._

```
Internet
    │
  Home Router (10.0.0.1)
    │   └── DHCP reservations for all fixed machines
    │
  Home LAN: 10.0.0.0/24
    │
    ├── MacBook Air (Wi-Fi): dynamic IP        ← mobile, not fixed
    ├── [other home devices]
    │
    └── Uplink cable → Basement Switch
                          │
                          ├── Mac Mini (Ethernet, en0): reserved IP   ← primary, Wi-Fi as backup
                          ├── Desktop (Ethernet):        reserved IP
                          ├── GPU3060 (Ethernet):        reserved IP
                          └── [future basement machines: additional switch ports]
```

**Design principles of the target topology:**
- All basement machines are on the same flat `10.0.0.x` LAN. No NAT between them.
- Each machine has a DHCP reservation at the router — MAC-to-IP binding. IPs do not drift.
- The Mac Mini's Ethernet becomes the primary interface. Wi-Fi becomes a backup fallback.
- The switch is a dumb Layer 2 switch — it does not route. The router handles routing and DHCP.
- Adding a new basement machine means plugging into the switch and reserving an IP at the router. No other changes required.
- The Mac Mini does not act as a gateway or NAT router for other machines. It is a peer on the same LAN.
- The OpenClaw gateway binds to the Mac Mini's LAN IP and is reachable from other basement machines and the MacBook Air over the home LAN.

**What is still not decided:**
- Whether the switch will also have a separate management VLAN or remain flat (flat is preferred for simplicity at this scale).
- Whether the Mac Mini will eventually expose the OpenClaw gateway beyond the home LAN (via Tailscale or similar). This is a Phase 3+ decision.
- Permanent network uplink routing between buildings or locations, if the org expands beyond this site.

**Warning:** Internet Sharing (Mac Mini Wi-Fi → Ethernet) is a possible short-term workaround but must not become the permanent architecture. If it is enabled temporarily, it must be explicitly removed when the switch is properly uplinked. Temporary workarounds that persist silently become permanent liabilities.

**Gateway LAN exposure:** Even after Ethernet is properly uplinked and the Mac Mini gets a stable LAN IP, directly exposing the OpenClaw gateway to the LAN (beyond loopback) remains a deliberate decision to be made — not an automatic consequence of network connectivity. In early phases, gateway access may intentionally remain local-only on the Mac Mini, with remote human access handled via Screen Sharing or SSH tunneling. LAN-wide gateway exposure is a Phase 1/2 configuration decision recorded in D1.9 BROKER_POLICY.md.

---

## 7. Tooling Topology

### Mac Mini

| Tool | Role |
|---|---|
| OpenClaw | Gateway host. bridge agent runtime. All other agents pair to this gateway. |
| Cursor | Available for remote editing sessions. Not the primary authoring machine. |
| Local LLM | Required for bridge-level tasks. Inference stays local unless routing policy says otherwise. Model TBD. |
| Mission Control workspace | Initial host. Path: `~/Cursor Projects/mission-control/`. May migrate to a dedicated server or shared path in later phases. |
| Telegram | bridge agent communicates via Telegram bot (SEC-002). Mac Mini must maintain the Telegram bot connection as long as bridge is active. Discord is optional and requires explicit activation per D1.11 §7.3. |

### Desktop

| Tool | Role |
|---|---|
| OpenClaw | brain agent runtime. Paired to Mac Mini gateway. |
| Cursor | Available. May be used for planning and memory-adjacent work. |
| Local LLM | Required. Emphasis on memory embedding, planning, and synthesis. GPU or CPU-only depending on Desktop hardware. |
| n8n | Execution substrate. Prior workflows to be triaged. Where retained, n8n executes tasks assigned by brain — it does not direct them. |
| Mission Control workspace | Read/write access. Not the host. |
| Telegram | brain agent results are delivered via bridge (Broker) through Telegram. Brain does not hold channel credentials directly. |

### GPU3060 Laptop

| Tool | Role |
|---|---|
| OpenClaw | forge agent runtime. Paired to Mac Mini gateway. |
| Cursor | Primary code editing surface for Builder role tasks. |
| Local LLM | Primary GPU-accelerated inference node. RTX 3060 drives high-load tasks. Model TBD — likely quantized Llama or Mistral family. |
| Mission Control workspace | Read/write access. Not the host. |
| Telegram | forge agent results are delivered via bridge (Broker) through Telegram. Forge does not hold channel credentials directly. |

### MacBook Air

| Tool | Role |
|---|---|
| OpenClaw | Client only. Used to issue directives and review status. Not a gateway or agent host. |
| Cursor | Primary human authoring environment. Document creation, code review, architecture decisions. |
| Local LLM | Optional lightweight inference for human-side drafting. Not a worker node. |
| Mission Control workspace | Read/write access to the workspace hosted on Mac Mini. Human authors and reviews from here; authoritative copy remains on Mac Mini. |
| Telegram | Human reviews agent reports and issues directives via Telegram. |

### Pixel / Seeker

| Tool | Role |
|---|---|
| OpenClaw | Device pairing to bridge. Not full agent runtime in early phases. |
| Local LLM | Not planned for early phases. |
| Telegram | Notification receipt and lightweight response via Telegram. |
| Mission Control | Read-only monitoring access. TBD. |

---

## 8. Local Inference Topology

Local inference is required on every worker machine. Cloud inference is a fallback, not a primary dependency. This is a founding architectural constraint, not a preference.

### Mac Mini — bridge inference

- Handles bridge-level reasoning: task routing, scheduling decisions, message triage, short planning tasks
- Inference load is expected to be moderate and bursty — not continuous
- Apple Silicon Neural Engine available for lightweight models
- Model selection: must run comfortably on Apple Silicon without degrading gateway responsiveness
- Model TBD — likely a small-to-medium quantized model (e.g. quantized Qwen, Phi, or Llama family)

### Desktop — brain inference

- Handles memory synthesis, planning, commerce reasoning, and knowledge indexing
- Inference load is moderate and sustained — brain is expected to process and index continuously
- Hardware: TBD (CPU-only vs GPU-capable — confirm on integration)
- Model TBD — must support embedding generation and coherent long-context reasoning
- If Desktop has a capable GPU, it takes over some load from GPU3060 for non-build tasks

### GPU3060 Laptop — forge inference

- Primary high-load inference node
- Handles code generation, QA reasoning, research summarization, and any task requiring extended context or fast iteration
- RTX 3060 drives GPU-accelerated inference
- Model TBD — likely a quantized 7B–13B model; possibly larger with aggressive quantization
- forge is the experimental inference surface — new models are tested here before being promoted to other machines

### Cloud inference — fallback and supplement

- Used when local models are insufficient for a specific task (context length, capability gap)
- Routing policy governs when cloud is invoked — see D1.5 MODEL_POLICY.md (to be written)
- Provider: Anthropic API (claude-sonnet-4-6 or equivalent) as primary cloud fallback
- Cloud inference costs money per token — the Cost/Usage lane in D1.1 tracks this
- Cloud inference must never be the silent default. It must be explicit in routing policy.

### Proxy-style routing (experimental — not a dependency)

There is a concept of routing inference requests across machines — e.g., bridge offloading heavy inference to forge via a local API proxy. This is architecturally interesting and worth exploring in the Experiment lane. It is **not** a dependency for Phase 1 or Phase 2. Do not build for it. Do not block on it.

---

## 9. Memory Topology

Memory in this system is not a single store. It is layered by purpose and machine.

### Canonical Memory — Desktop (brain)

- The authoritative knowledge base of the organization
- Contains: indexed research, project history, decisions, world model, commerce state, prompt packs
- Managed by brain (Archivist role)
- Must be backed up. Data loss here is organizational loss.
- Indexing method: TBD (vector store, graph, hybrid — to be specified in D1.7 MEMORY_POLICY.md)

### Operational Memory — Mac Mini (bridge)

- Task queue state, routing decisions, session logs, scheduling history
- Managed by bridge (Conductor role)
- Path: `~/.openclaw/agents/main/` (currently default agent; will be bridge agent once defined)
- Session store: `~/.openclaw/agents/main/sessions/`
- Shorter retention — operational logs, not permanent knowledge
- Promotes important decisions and outcomes to canonical memory via brain

### Working Memory — GPU3060 (forge)

- Active build context, QA results, research packets in progress, experiment logs
- Managed by forge (Builder, Researcher roles)
- Short-lived by design — outputs are the artifact, not the working state
- Experiment outcomes that are adopted promote into canonical memory via brain
- Experiment outcomes that are retired go into D5.4 EXPERIMENT-REGISTRY.md

### Edge Receipts — Pixel / Seeker

- Minimal local state only — task receipts, notification logs
- Not a memory store
- Nothing important lives here permanently

### Memory Promotion Path

```
forge working memory
    → (on adoption/completion) → brain canonical memory

bridge operational memory
    → (on significant decisions) → brain canonical memory

edge receipts
    → (nothing promoted — ephemeral only)

brain canonical memory
    → (source of truth for org knowledge)
    → (read by all agents as needed)
```

---

## 10. Mission Control Architecture

### Where It Lives First

Mission Control is hosted on the Mac Mini at:
```
~/Cursor Projects/mission-control/
```

This is the current working location. It may migrate in later phases if a better host is identified (e.g., a shared volume, a dedicated lightweight server, or a git-backed remote). Until then, it lives on the Mac Mini, which is always on and always reachable.

The `docs/` subdirectory holds the canonical D-series document set. Additional subdirectories will be added as Mission Control grows (e.g., `tasks/`, `status/`, `registry/`, `proofs/`).

### MVP Contents

The first version of Mission Control must contain and keep current:

| Element | What it tracks |
|---|---|
| Task queue | Active, queued, and blocked tasks with owner and status |
| Machine status | Which nodes are online and what they are running |
| Document set | Which D-series documents exist, their version, and completeness status |
| Delegation status | Which task types are delegated and at what maturity level (0–5) |
| Proof links | References to output artifacts and logs from completed delegations |
| Improvement intake | Incoming research signals, experiments queued for review |
| Experiment registry | Log of experiments run, outcomes, and adoption decisions |

### Implementation Form

The MVP form is a structured directory with markdown files and optionally a lightweight status dashboard. A full web UI is deferred. The design principle is: **legibility before beauty**. An agent or a human must be able to read Mission Control's current state in under two minutes without a GUI.

Specific implementation choices (static site, Notion, custom app, etc.) are deferred to D1.13 ENV_CONTROL.md and the Phase 1 architecture review.

---

## 11. Communications Architecture

### Telegram — Primary Communication Channel

- Primary communication channel between agents and human (D1.11 §3, §7.1)
- Bridge posts results, digests, alerts, and status updates via the Telegram bot (SEC-002)
- Human issues directives via Telegram for bridge to receive and route
- Telegram is **not** the source of truth — it is the delivery mechanism for notifications and directives
- Telegram state is ephemeral relative to Mission Control — what matters is filed in Mission Control, not left in chat

### Mission Control — Source of Truth

- All task assignments, delegations, plans, and outcomes are recorded in Mission Control
- Telegram messages that contain decisions or outputs must be reflected in Mission Control
- Agents do not treat Telegram history as authoritative — they treat Mission Control documents as authoritative

### Design Principle: Minimal Channel Footprint

- Operational messages route through one primary channel (ops-channel)
- Alerts are distinct from digests; escalations are visually marked
- Channel structure details are governed by D1.11; this document defines the architecture intent only

### Discord

Discord is optional. It is not the primary channel and is not activated by default. Discord may be configured as an explicitly approved secondary channel only if the human decides to activate it, with the bot token registered per D1.14, and usage rules documented in D1.11 and D1.9. Discord is not a fallback for Telegram outages — see D1.9 §3 Principle 1 and D1.11 §7.3.

---

## 12. Capability Architecture

Capabilities are acquired in layers, gated in order. No new capability type should be adopted before the previous layer is stable.

```
Layer 1 — Built-in tools        (OpenClaw native: browser, exec, memory, cron, files)
Layer 2 — Skills / plugins      (OpenClaw skill registry, community skills, custom skills)
Layer 3 — Brokered tools        (API integrations, webhooks, third-party services via bridge)
Layer 4 — Automation substrates (n8n workflows, browser automation, custom scripts)
Layer 5 — Custom integrations   (built-in-house for specific org needs)
```

**Rules:**
- A new capability must have a clear owner agent and a documented purpose before it is activated
- Capability acquisition is recorded in D5.2 SKILL-REGISTRY.md
- Capability adoption decisions go through the Skill/Plugin lane in D1.1
- No capability is permanently installed without a deliberate adoption decision
- Capabilities can be retired — retirement is recorded

**Tool Lane Architecture:**

Three designated tool lanes serve specific task classes within this capability model. Agents self-manage lane selection within approved policy:

| Lane | Tool | Category | Task Classes | Current Status |
|---|---|---|---|---|
| Orchestration | OpenClaw | CAP-INF (active) | Primary autonomous runtime. Task dispatch, scheduling, gateway, inter-agent routing. Default lane for approved backlog work and Mission Control buildout. | Active — Phase 1 |
| Engineering (supervised) | Cursor | CAP-SKL (active) | Supervised/manual engineering lane. Code, QA, document authoring, configuration. Requires human-in-the-loop or explicit task assignment. Not the autonomous runtime. May be operated indirectly via device-mediated control. | Active — Phase 1 (supervised) |
| Live Research / UI-Pilot | Perplexity | CAP-SVC (candidate) | Live web retrieval, current information, browser/computer-use. Preferred UI-pilot lane for app/device/chatbox interaction including indirect Cursor usage. | Candidate — D5.7 CAP-006 |

**OpenClaw is the primary autonomous runtime.** Approved backlog work defaults to OpenClaw. Cursor is the supervised/manual engineering surface — not the swarm's autonomous executor. Cursor may be operated indirectly through device-mediated control (Perplexity or OpenClaw navigating the Cursor UI) without requiring direct API integration. This is a documented access path.

**Perplexity is the preferred UI-pilot lane** for tasks requiring browser, app, device, or chatbox interaction. OpenClaw orchestrates; Perplexity operates UI-first tools when that is the best access path.

Perplexity is a callable external service invoked by agents during task execution — not a runtime agent, not a model, and not an orchestration layer. It produces outputs (research packets, web facts) that travel through bridge (Broker) before reaching human-facing channels or canonical memory. Activation requires the D1.12 adoption process and a D1.15 change record.

---

## 13. Environment Architecture

### Stable vs. Experiment

| Environment | Purpose | Location |
|---|---|---|
| Production | Live org operation — agents running real tasks | Mac Mini (bridge), Desktop (brain), GPU3060 (forge) |
| Sandbox | Experiments, model tests, new tool evaluation | GPU3060 (forge) — isolated from production data |
| Dev | Local testing of new code or agent config before promotion | MacBook Air or dedicated forge path |

### Machine Responsibility Boundaries

- Mac Mini: production gateway and bridge only. Experiments that could affect gateway stability do not run here.
- Desktop: canonical memory is production. Separate experiment path for brain research tasks.
- GPU3060: primary experiment surface. Safe to break here. Outputs are promoted to production only after validation.

### No Secret Leakage

Production secrets (API keys, tokens, credentials) do not enter experiment environments. Experiment environments use test credentials or no credentials. This is enforced by configuration, not trust.

See D1.14 SECRETS_POLICY.md (to be written) for full secrets management design.

---

## 14. Paths, Ownership, and System Homes

Known paths are documented here. Unknowns are marked TBD.

| Resource | Path / Location | Owner | Status |
|---|---|---|---|
| Mission Control root | `~/Cursor Projects/mission-control/` | Mac Mini (authoritative host) | Active |
| Mission Control docs | `~/Cursor Projects/mission-control/docs/` | Mac Mini (authoritative host) | Active |
| Mission Control version control | git repository expected at Mission Control root — tracks all doc, task, and registry changes over time | Mac Mini | Not yet initialized |
| OpenClaw state — Mac Mini | `~/.openclaw/` | Mac Mini | Active |
| OpenClaw config — Mac Mini | `~/.openclaw/openclaw.json` | Mac Mini | Active |
| OpenClaw gateway plist | `~/Library/LaunchAgents/ai.openclaw.gateway.plist` | Mac Mini | Active |
| OpenClaw gateway logs | `~/.openclaw/logs/gateway.log` | Mac Mini | Active |
| OpenClaw session store | `~/.openclaw/agents/main/sessions/` | Mac Mini | Active (default agent) |
| Node 22 runtime | `~/.local/node22/bin/` | Mac Mini | Active |
| OpenClaw binary | `~/.local/node22/bin/openclaw` | Mac Mini | Active |
| Canonical memory store | Desktop — path TBD | Desktop (brain) | Not yet created |
| brain agent workspace | Desktop — path TBD | Desktop (brain) | Not yet created |
| brain OpenClaw state | Desktop — path TBD | Desktop (brain) | Not yet created |
| forge agent workspace | GPU3060 — path TBD | GPU3060 (forge) | Not yet created |
| forge OpenClaw state | GPU3060 — path TBD | GPU3060 (forge) | Not yet created |
| Local LLM model files — Mac Mini | TBD | Mac Mini | Not yet installed |
| Local LLM model files — Desktop | TBD | Desktop | Not yet installed |
| Local LLM model files — GPU3060 | TBD | GPU3060 | Not yet installed |

Paths on Desktop and GPU3060 will be established during Phase 2 integration and recorded here. Do not invent paths before the machines are online and their OS is confirmed.

---

## 15. Phase Binding

Each roadmap phase primarily activates a specific layer of the architecture.

| Phase | Name | Primary Architecture Layer Activated |
|---|---|---|
| Phase 0 | Foundation | Mac Mini — power, network, remote access, gateway baseline |
| Phase 1 | Bridge Online | Mac Mini — bridge agent, model config, Telegram, Mission Control MVP |
| Phase 2 | Cluster Connected | Basement network — switch uplink, Desktop and GPU3060 on LAN; brain and forge agents |
| Phase 3 | First Delegation | Inter-agent routing — bridge → brain and bridge → forge task flow |
| Phase 4 | Continuous Operation | Full org loop — daily digest, intake lanes, automatic recovery |
| Phase 5 | Scale and Extend | Edge nodes (Pixel, Seeker), additional capabilities, commerce activation |

No phase activates cleanly before the previous phase's architecture layer is stable. Phases can overlap at the margin — Phase 1 and Phase 0 completion may be concurrent — but the ordering of what gets built is fixed.

---

## 16. Guardrails

These are the non-negotiable constraints this architecture enforces. They do not change without a deliberate architectural decision recorded in this document.

1. **Three real agents at launch.** bridge, brain, forge. No fourth agent without explicit decision.
2. **Logical roles are not separate agents.** Conductor, Broker, Archivist, and others live inside existing agents. They are not spawned as new processes.
3. **Local inference runs on every worker machine.** Mac Mini, Desktop, GPU3060 all run local models. Cloud is the fallback, not the default.
4. **Mission Control is the source of truth.** Telegram is the primary delivery channel. n8n is an execution substrate. Neither replaces Mission Control.
5. **Temporary workarounds must not persist.** If a hack is needed (e.g., Internet Sharing, a manual IP assignment), it is documented as temporary and has an explicit exit condition.
6. **Documents precede implementation.** New capabilities, new agents, and new integrations require documentation before build. The spec is not retroactive.
7. **The MacBook Air is not a server.** Org operation does not depend on it being awake.
8. **Pixel and Seeker are edge surfaces, not agents.** They do not host runtime agents in early phases.

---

## 17. Deferred Decisions

These questions are explicitly open. They are not answered here. They will be answered in specific later documents or phase reviews.

| Decision | Deferred To |
|---|---|
| Pixel and Seeker final form and agent pairing model | Phase 5 design / D1.4 ORG_MAP.md update |
| Permanent network backhaul — whether the basement LAN will ever be separated from the home LAN | Phase 2 network review |
| Whether to expose OpenClaw gateway beyond home LAN (Tailscale or similar) | Phase 3 / D1.9 BROKER_POLICY.md |
| Final Mission Control UI implementation form | Phase 1 exit review / D1.13 ENV_CONTROL.md |
| Exact commerce subsystem implementation — platform, tooling, automation form | D3.5 DELEGATION-SPEC-COMMERCE.md |
| When multimodal memory R&D becomes an active workstream | D1.16 MEMORY_RND.md — after Phase 3 is stable |
| Desktop OS and exact hardware specs | Phase 2 integration |
| GPU3060 laptop OS and exact local inference stack | Phase 2 integration |
| Whether Desktop has a GPU usable for inference | Phase 2 integration |
| Final local LLM model selection per machine | D1.6 LOCAL_INFERENCE_PLAN.md |
| Memory store technology (vector DB, graph, hybrid) | D1.7 MEMORY_POLICY.md |
| Secrets management tooling | D1.14 SECRETS_POLICY.md |

---

## 18. Immediate Architectural Next Steps

In order, following D1.1-ROADMAP.md §11:

1. **Write D1.3 — REQUIREMENTS.md**
   Define functional requirements for bridge, brain, and forge. Define acceptance criteria per phase. This is the test spec that validates the architecture is actually working.

2. **Write D1.4 — ORG_MAP.md**
   Map ownership, decision rights, role-to-agent binding, and how the five outcome tracks relate to the three runtime agents. Clarifies who owns what when tasks are in flight.

3. **Write D1.5 — MODEL_POLICY.md**
   Define which model runs on which machine, when cloud inference is permitted, how routing decisions are made, and what the cost controls are. Do not configure model providers before this is written.

4. **Inventory legacy assets on Desktop**
   The Desktop cannot be properly integrated until its existing state is known. n8n workflows, bot configs, prompt packs, and commerce automation must be surveyed. Results go into D5.3 LEGACY-ASSET-REGISTER.md.

5. **Uplink the basement switch**
   One cable from switch to router. Verify all three machines get 10.0.0.x addresses. Set DHCP reservations. Update §5 of this document to reflect the new current state.

6. **Define the bridge agent in OpenClaw**
   `openclaw agents add bridge` — after D1.3 and D1.4 exist to define what bridge's workspace and role config should contain.

---

_This document describes the architecture as it is and as it is intended to become. It is updated when machines are added, network paths change, or structural decisions are made. All downstream documents that reference machine topology, agent placement, or path ownership must be reviewed when this file changes._
